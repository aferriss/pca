cmake_minimum_required(VERSION 2.8)
project( PCATest )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( PCATest pca.cpp )
target_link_libraries( PCATest ${OpenCV_LIBS} )